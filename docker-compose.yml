services:
  agent-fiscal:
    build: .
    container_name: agent-fiscal-bot
    restart: always
    env_file:
      - .env
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
